// Package commands implements the krewire CLI sub-commands.
// Each command lives in its own file per Service/Unit scope
// (version.go, info.go, new.go, toolchain.go, helpers.go) to avoid the God Module anti-pattern.
package commands
